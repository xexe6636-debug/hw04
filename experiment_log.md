# 实验记录（experiment_log.md）

## 一、实验环境

| 项目 | 内容 |
|------|------|
| 操作系统 | Windows 11 Home 23H2 / macOS Sonoma 14.x（请填写实际系统） |
| Python 版本 | 3.10.x（请填写实际版本，可用 `python --version` 查看） |
| CPU | （请填写，如 Intel Core i5-12500H） |
| 内存 | （请填写，如 16 GB） |
| GPU | 无（纯 CPU 推理） / 有（如 NVIDIA RTX 3060，CUDA 12.x） |
| Whisper 版本 | openai-whisper 20231117 |
| 所用模型 | base（约 140 MB） |

---

## 二、测试音频说明

| 文件名 | 来源 | 时长 | 内容 |
|--------|------|------|------|
| `tts_output.mp3` | 剪映 TTS 导出（任务二） | 约 3 分钟 | 《人工智能正在悄悄重塑我们的学习方式》全文 |

> 若使用麦克风录制，请在此填写录音内容描述、时长、环境噪音情况等。

---

## 三、识别样例

### 3.1 文件模式识别结果（节选）

**原文（前两句）：**
> 从背单词的 App 到个性化推题系统，从课堂录像自动生成字幕到 AI 助教随时答疑，人工智能正以一种几乎不被察觉的方式，深刻改变着我们获取知识的每一个环节。

**Whisper base 识别结果（前两句，请运行后填写）：**
```
（请将 transcript 文件中对应段落粘贴至此）
```

**观察**：
- 标点恢复：（请填写，如"逗号、句号基本正确，少数处有误"）
- 专有名词：（如"AI"识别为"A.I."或"人工智能"）
- 错误字例：（如"重塑"识别为"重塑"，正确；或其他错误）

---

### 3.2 分段时间戳示例

```
（请粘贴运行后终端打印的时间戳内容，格式如下）
[  0.00s →  5.20s]  从背单词的App到个性化推题系统……
[  5.20s → 10.40s]  ……
```

---

## 四、性能观察

| 指标 | 数值 |
|------|------|
| 音频时长 | 约 3 分钟 |
| 识别耗时（CPU） | （请填写，如 "约 45 秒"） |
| 实时率（RTF） | （耗时 / 音频时长，如 0.25 = 快 4 倍） |
| 词错误率（粗估） | （人工对比原文，估算错误字数 / 总字数） |

---

## 五、遇到的问题与解决方法

| 问题 | 原因 | 解决方法 |
|------|------|----------|
| `No module named 'whisper'` | 未安装依赖 | `pip install openai-whisper` |
| `ffmpeg not found` | 系统未安装 ffmpeg | Windows：`winget install ffmpeg`；macOS：`brew install ffmpeg` |
| 首次运行下载很慢 | 模型文件需从 HuggingFace 下载 | 可开代理或预先手动下载放入 `~/.cache/whisper/` |
| （其他问题请自行补充） | | |

---

## 六、总结

Whisper base 模型在纯 CPU 环境下对标准普通话的识别效果**令人满意**，主要误差集中在专有名词和轻声词汇。对于本次任务（标准 TTS 语音），准确率估计在 95% 以上。

若需进一步提升：
- 换用 `small` 或 `medium` 模型，准确率可提升约 2～5%，但速度下降约 2～4 倍。
- 在有 CUDA GPU 的环境下，`large-v3` 模型可在 1× 实时内完成识别。
